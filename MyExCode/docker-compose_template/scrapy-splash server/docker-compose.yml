version: '3'
services:
    scrapy:
        container_name: scrapy
        image: scrapinghub/splash
        ports:
            - 8050:8050
        volumes:
            - ../spiders:/usr/src/app
        # 為了不讓他中止
        command: ping 127.0.0.1